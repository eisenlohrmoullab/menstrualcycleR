% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cycledata_check.R
\name{cycledata_check}
\alias{cycledata_check}
\title{Analyze Non-Missing Symptom Data by Cycle Phase}
\usage{
cycledata_check(data, symptom_columns)
}
\arguments{
\item{data}{A data frame containing the input data.
\itemize{
\item Must include columns for scaled cycle day variables and the symptom or dependent variables of interest.
}}

\item{symptom_columns}{A character vector of column names representing the dependent variables of interest.
For example, \code{c("symptom1", "symptom2", "symptom3")}, where each name corresponds to a column in your data set.}
}
\value{
A list containing three components:
\itemize{
\item \strong{\code{by_id}}: A data frame summarizing non-missing values for each ID and symptom, including:
\enumerate{
\item Total number of non-missing values.
\item Number of non-missing values during the luteal phase.
\item Number of non-missing values during the follicular phase.
}
\item \strong{\code{overall}}: A data frame summarizing non-missing values for each symptom, including:
\enumerate{
\item Total number of non-missing values.
\item Number of non-missing values during the luteal phase.
\item Number of non-missing values during the follicular phase.
}
\item \strong{Plots}: Visualizations showing the availability of non-missing data for each symptom, stratified by ID.
}
}
\description{
This function provides detailed information about the availability of non-missing symptom data, stratified by
cycle phases (luteal and follicular) and overall availability. It also generates visualizations to aid in data
quality assessment.
}
\examples{
# Load example dataset
data(cycledata)

data_with_scaling <- pacts_scaling(
  data, 
  id = id, 
  date = daterated, 
  menses = menses, 
  ovtoday = ovtoday, 
  lower_cyclength_bound = 21, 
  upper_cyclength_bound = 35
)

# Analyze symptom data availability
data_available_info <- cycledata_check(
  data_with_scaling, 
  symptom_columns = c("symptom")
)

# View results
print(data_available_info$by_id)
print(data_available_info$overall)
print(data_available_info$data_symptom_plots$symptom)
}
\keyword{cycle}
\keyword{cycle,}
\keyword{data}
\keyword{menstrual}
\keyword{phase}
\keyword{quality,}
\keyword{symptoms,}
